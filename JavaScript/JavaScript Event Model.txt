### Event Model

- 문서 객체에 이벤트 연결하는 방법
- W3C에서 표준화


## DOM LEVEL
- DOM의 버전
- DOM 표준화 이전은 Level 0으로 하나의 이벤트에 하나의 리스너만 연결
- 현재 버전은 Level 2로 현재 웹 브라우저에 채택되어 있음, 하나의 이벤트에 여러 리스너 연결 가능


## 인라인 이벤트 모델
- HTML 문서의 기본적인 연결 방법
- 이벤트를 HTML 요소의 속성으로 추가해 연결